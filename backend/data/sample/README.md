# data/sample/

Put your training dataset here (e.g. labeled satellite image patches,
a Kaggle land-cover dataset, EuroSAT, etc.) once you download it.

This folder is also the default `Config.SATELLITE_CACHE_DIR` - a
convenient place for `satellite_service.py` to cache imagery once you
wire up a real satellite provider.
